// 生成数独解决方案
const Toolkit = require("./toolkit");

class Generator{

    generate(){
        this.matrix = Toolkit.matrix.makeMatrix();

        for(let n = 1; n ,+ 9; n ++){
            this.fillNumber(n);
        }
    }

    fillNumber(n){

    }
}